# The default simulated provider needs no environment variables.
AGENT_ATELIER_PROVIDER=simulated

# Optional real provider. Copy this file to .env only if your launcher loads it,
# or set the variables in your shell. Never commit an actual key.
OPENAI_API_KEY=
AGENT_ATELIER_MODEL=
